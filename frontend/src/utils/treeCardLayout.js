/** Shared card geometry for layout hubs, handles and export. Keep in sync with .person-node CSS. */
export const PERSON_CARD = {
  width: 184,
  height: 214,
  /** px from card top to left/right handle (= avatar center). */
  sideHandleY: 50,
  hubSize: 10,
};
